This is a simple markdown file without any front matter.

It should still be processed correctly by kiss and get default values.

## Features

- No YAML front matter
- Pure markdown content
- Should inherit from parent index files
